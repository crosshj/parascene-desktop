# Public (safe to expose in frontend / binary)
VITE_PARASCENE_BASE_URL=https://www.parascene.com
# Optional override — default is baked in src/sdk/config.ts
# VITE_PARASCENE_CLIENT_ID=c7826d84-92b2-42b5-92db-473662b51a77

# Register this exact redirect on the Parascene app (Connections):
# http://127.0.0.1:17423/oauth/callback
#
# Mark the app as Native / public client so token exchange uses PKCE only.
